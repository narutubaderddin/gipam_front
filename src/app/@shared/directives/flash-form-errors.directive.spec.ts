import { FlashFormErrorsDirective } from './flash-form-errors.directive';

describe('FlashFormErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new FlashFormErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
