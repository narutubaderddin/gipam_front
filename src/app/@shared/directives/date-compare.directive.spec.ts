import { DateCompareDirective } from './date-compare.directive';

describe('DateCompareDirective', () => {
  it('should create an instance', () => {
    const directive = new DateCompareDirective();
    expect(directive).toBeTruthy();
  });
});
