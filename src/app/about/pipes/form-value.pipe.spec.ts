import { FormValuePipe } from './form-value.pipe';

describe('FormValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FormValuePipe();
    expect(pipe).toBeTruthy();
  });
});
